package mokiyoki.enhancedanimals.items;

public class CustomizableSaddle extends CustomizableAnimalEquipment {
    public CustomizableSaddle(Properties builder, int originalColour) {
        super(builder, originalColour);
    }
}
