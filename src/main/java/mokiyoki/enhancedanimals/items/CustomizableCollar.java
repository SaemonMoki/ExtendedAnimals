package mokiyoki.enhancedanimals.items;

public class CustomizableCollar extends CustomizableAnimalEquipment{
    public CustomizableCollar(Properties builder, int originalColour) {
        super(builder, originalColour);
    }
}
