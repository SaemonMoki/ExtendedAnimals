package mokiyoki.enhancedanimals.items;

public class SpikedCollar extends CustomizableCollar{
    public SpikedCollar(Properties builder, int originalColour) {
        super(builder, originalColour, false);
    }
}
