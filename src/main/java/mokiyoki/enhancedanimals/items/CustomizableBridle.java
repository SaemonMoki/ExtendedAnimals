package mokiyoki.enhancedanimals.items;

import net.minecraft.world.item.Item.Properties;

public class CustomizableBridle extends CustomizableAnimalEquipment{
    public CustomizableBridle(Properties builder, int originalColour) {
        super(builder, originalColour);
    }
}
